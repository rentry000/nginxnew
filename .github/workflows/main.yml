name: Build Custom Caddy ARM64 (Latest v2.11.1)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-caddy:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go_version: ["1.25"]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go Environment
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go_version }}

      - name: Install xcaddy (pre-built amd64)
        run: |
          wget https://github.com/caddyserver/xcaddy/releases/download/v0.4.5/xcaddy_0.4.5_linux_amd64.tar.gz
          tar -xzf xcaddy_0.4.5_linux_amd64.tar.gz
          chmod +x xcaddy
          ./xcaddy version

      - name: Clone CrowdSec Plugin
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN_PAT }}
        run: |
          git clone https://$GH_TOKEN@github.com/hslatman/caddy-crowdsec-bouncer.git crowdsec-plugin
          cd crowdsec-plugin
          git checkout main || true
          cd ..

      - name: Clone W0n9/caddy_waf_t1k and chaitin/t1k-go Locally
        run: |
          git clone https://github.com/W0n9/caddy_waf_t1k.git caddy_waf_t1k
          cd caddy_waf_t1k
          git checkout master || true
          cd ..

          git clone https://github.com/chaitin/t1k-go.git t1k-go
          cd t1k-go
          git checkout master || true
          cd ..

      - name: Pre-download and Fix watcher-go Headers
        run: |
          echo "=== Fixing watcher-c.h for ARM64 (printf patch) ==="
          go mod init dummy || true
          go get github.com/e-dant/watcher@v0.0.0-20260223030516-06f84a1314be || true
          
          WATCHER_PATH=$(go list -m -f {{.Dir}} github.com/e-dant/watcher)
          echo "Watcher path: $WATCHER_PATH"
          
          if [ -n "$WATCHER_PATH" ]; then
            sudo chmod -R +w "$WATCHER_PATH"
            
            mkdir -p "$WATCHER_PATH/watcher-go/wtr"
            cp -r "$WATCHER_PATH/include/wtr/"* "$WATCHER_PATH/watcher-go/wtr/" 2>/dev/null || true
            
            if [ ! -f "$WATCHER_PATH/watcher-go/wtr/watcher-c.h" ]; then
              echo "Creating dummy watcher-c.h with printf..."
              printf "/* Dummy header for ARM64 cross-compile */\n#ifndef WTR_WATCHER_C_H\n#define WTR_WATCHER_C_H\ntypedef void* watcher_t;\nwatcher_t watcher_new(const char* path);\nvoid watcher_destroy(watcher_t w);\n#endif\n" > "$WATCHER_PATH/watcher-go/wtr/watcher-c.h"
            fi
            
            echo "watcher patch completed"
            ls -l "$WATCHER_PATH/watcher-go/wtr/" || true
          else
            echo "Watcher path not found!"
          fi

      - name: Clean Go Module Cache
        run: go clean -modcache

      - name: Install ARM64 Cross Compiler and Python Headers
        run: |
          # 1. 允许 arm64 架构
          sudo dpkg --add-architecture arm64

          # 2. 修改 sources.list 以支持 arm64 架构的 ports 源
          # 我们为 arm64 专门指定 ports.ubuntu.com 镜像
          sudo sed -i 's/http:\/\/archive.ubuntu.com\/ubuntu\//[arch=amd64] http:\/\/archive.ubuntu.com\/ubuntu\//g' /etc/apt/sources.list
          sudo sed -i 's/http:\/\/security.ubuntu.com\/ubuntu\//[arch=amd64] http:\/\/security.ubuntu.com\/ubuntu\//g' /etc/apt/sources.list
          
          # 添加针对 arm64 的官方 ports 源 (Noble 24.04)
          echo "deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports noble main restricted universe multiverse" | sudo tee -a /etc/apt/sources.list
          echo "deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports noble-updates main restricted universe multiverse" | sudo tee -a /etc/apt/sources.list
          echo "deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports noble-security main restricted universe multiverse" | sudo tee -a /etc/apt/sources.list

          # 3. 更新并安装
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu g++-aarch64-linux-gnu libc6-dev-arm64-cross pkg-config build-essential
          sudo apt-get install -y libpython3-dev:arm64 python3-all-dev:arm64

      - name: Build Custom Caddy ARM64 with FrankenPHP
        env:
          GOOS: linux
          GOARCH: arm64
          GOPROXY: https://goproxy.cn,direct
          CGO_ENABLED: 1
          CC: aarch64-linux-gnu-gcc
          CXX: aarch64-linux-gnu-g++
          # 修正 Python 头文件路径，动态指向 arm64 目录
          PKG_CONFIG_PATH: /usr/lib/aarch64-linux-gnu/pkgconfig
          CGO_CPPFLAGS: "-I/home/runner/go/pkg/mod/github.com/e-dant/watcher@v0.0.0-20260223030516-06f84a1314be/watcher-go/wtr -I/usr/include/python3.12"
          CGO_CFLAGS: "-I/home/runner/go/pkg/mod/github.com/e-dant/watcher@v0.0.0-20260223030516-06f84a1314be/watcher-go/wtr -I/usr/include/aarch64-linux-gnu/python3.12"
          CGO_LDFLAGS: "-L/usr/lib/aarch64-linux-gnu"
        run: |
          ./xcaddy build v2.11.2 \
            --with github.com/mliezun/caddy-snake \
            --with github.com/mholt/caddy-webdav \
            --with github.com/caddy-dns/route53@v1.6.0 \
            --with github.com/caddyserver/replace-response \
            --with github.com/caddyserver/transform-encoder \
            --with github.com/caddyserver/cache-handler \
            --with github.com/caddy-dns/cloudflare \
            --with github.com/caddy-dns/duckdns \
            --with github.com/greenpau/caddy-security \
            --with github.com/mholt/caddy-l4 \
            --with github.com/caddyserver/nginx-adapter \
            --with github.com/caddyserver/ntlm-transport \
            --with github.com/mholt/caddy-dynamicdns \
            --with github.com/sagikazarmark/caddy-fs-s3 \
            --with github.com/ueffel/caddy-brotli \
            --with github.com/corazawaf/coraza-caddy/v2 \
            --with github.com/mholt/caddy-ratelimit \
            --with github.com/invzhi/caddy-docker-upstreams \
            --with github.com/hslatman/caddy-crowdsec-bouncer \
            --with github.com/mholt/caddy-events-exec \
            --with github.com/caddyserver/forwardproxy \
            --with github.com/kirsch33/realip \
            --with github.com/sjtug/caddy2-filter \
            --with github.com/lucaslorentz/caddy-docker-proxy/v2 \
            --with github.com/caddyserver/circuitbreaker \
            --with github.com/caddyserver/jsonc-adapter \
            --with github.com/caddyserver/json5-adapter \
            --with github.com/SakuraHentai/caddy-dynamic-routing \
            --with github.com/ysicing/caddy2-geocn \
            --with github.com/steffenbusch/caddy-placeholder-dump \
            --with github.com/hslatman/caddy-crowdsec-bouncer/appsec \
            --with github.com/hslatman/caddy-crowdsec-bouncer/layer4 \
            --with github.com/libdns/cloudflare \
            --with github.com/porech/caddy-maxmind-geolocation \
            --with github.com/hslatman/caddy-crowdsec-bouncer/http \
            --with pkg.jsn.cam/caddy-defender@v0.10.0 \
            --replace github.com/W0n9/caddy_waf_t1k=./caddy_waf_t1k \
            --replace github.com/chaitin/t1k-go=./t1k-go \
            --replace github.com/hslatman/caddy-crowdsec-bouncer=./crowdsec-plugin \
            --output ./caddy-arm64

      - name: Show Build Result Info
        run: |
          file ./caddy-arm64 || true
          ls -lh ./caddy-arm64 || true
          sha256sum ./caddy-arm64 || true

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: caddy-arm64-custom-v2.11.1
          path: ./caddy-arm64
          retention-days: 14

      - name: Publish to GitHub Releases (on main branch)
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v2
        with:
          files: ./caddy-arm64
          tag_name: caddy-arm64-v$(./caddy-arm64 version --short 2>/dev/null | tr -d '\n\r' || echo "unknown")
          name: Custom Caddy ARM64 v2.11.1
          draft: false
          prerelease: false
          body: |
            Custom cross-compiled Caddy v2.11.2 for linux/arm64 (aarch64).
            Built with xcaddy v0.4.5 on Go ${{ matrix.go_version }}.
