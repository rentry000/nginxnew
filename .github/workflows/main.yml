name: Build Custom Caddy ARM64 (Latest v2.11.1)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-caddy:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go_version: ["1.23"]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go Environment
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go_version }}

      - name: Install xcaddy (pre-built amd64)
        run: |
          wget https://github.com/caddyserver/xcaddy/releases/download/v0.4.5/xcaddy_0.4.5_linux_amd64.tar.gz
          tar -xzf xcaddy_0.4.5_linux_amd64.tar.gz
          chmod +x xcaddy
          ./xcaddy version   # 只打印 xcaddy 版本，确认工具可用

      - name: Build Custom Caddy ARM64
        run: |
          GOOS=linux GOARCH=arm64 \
            ./xcaddy build v2.11.1 \
              --with github.com/fabriziosalmi/caddy-waf \
              --with github.com/mholt/caddy-webdav \
              --with github.com/caddy-dns/route53@v1.6.0 \
              --with github.com/caddyserver/replace-response \
              --with github.com/caddyserver/transform-encoder \
              --with github.com/caddyserver/cache-handler \
              --with github.com/caddy-dns/cloudflare \
              --with github.com/caddy-dns/duckdns \
              --with github.com/greenpau/caddy-security \   # ← 替换旧的 jwt/portal
              --with github.com/mholt/caddy-l4 \
              --with github.com/caddyserver/nginx-adapter \
              --with github.com/caddyserver/ntlm-transport \
              --with github.com/mholt/caddy-dynamicdns \
              --with github.com/sagikazarmark/caddy-fs-s3 \
              --with github.com/ueffel/caddy-brotli \
              --with github.com/corazawaf/coraza-caddy/v2 \
              --with github.com/mholt/caddy-ratelimit \
              --with github.com/invzhi/caddy-docker-upstreams \
              --with github.com/hslatman/caddy-crowdsec-bouncer \
              --with github.com/mholt/caddy-events-exec \
              --with github.com/caddyserver/forwardproxy \
              --with github.com/kirsch33/realip \
              --with github.com/mastercactapus/caddy2-proxyprotocol \
              --with github.com/sjtug/caddy2-filter \
              --with github.com/lucaslorentz/caddy-docker-proxy/v2 \
              --with github.com/caddyserver/circuitbreaker \
              --with github.com/caddyserver/jsonc \
              --with github.com/caddyserver/json5 \
              --with github.com/WeidiDeng/caddy-prometheus \
              --with github.com/serfriz/caddy-security-headers \
              --with github.com/RussellLuo/caddy-ext \
              --output ./caddy-arm64

      - name: Show Build Result Info (optional, safe on amd64 host)
        run: |
          file ./caddy-arm64
          ls -lh ./caddy-arm64
          sha256sum ./caddy-arm64 || true

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: caddy-arm64-custom-v2.11.1
          path: ./caddy-arm64
          retention-days: 14

      - name: Publish to GitHub Releases (on main branch)
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v2
        with:
          files: ./caddy-arm64
          tag_name: caddy-arm64-v$(./caddy-arm64 version --short 2>/dev/null | tr -d '\n\r' || echo "unknown")
          name: Custom Caddy ARM64 v2.11.1
          draft: false
          prerelease: false
          body: |
            Custom cross-compiled build of **Caddy v2.11.1** for linux/arm64 (aarch64) with many popular plugins.

            **Included plugins:**
            - github.com/fabriziosalmi/caddy-waf
            - github.com/mholt/caddy-webdav
            - github.com/caddy-dns/route53@v1.6.0
            - github.com/caddyserver/replace-response
            - github.com/caddyserver/transform-encoder
            - github.com/caddyserver/cache-handler
            - github.com/caddy-dns/cloudflare
            - github.com/caddy-dns/duckdns
            - github.com/greenpau/caddy-security
            - github.com/mholt/caddy-l4
            - github.com/caddyserver/nginx-adapter
            - github.com/caddyserver/ntlm-transport
            - github.com/mholt/caddy-dynamicdns
            - github.com/sagikazarmark/caddy-fs-s3
            - github.com/ueffel/caddy-brotli
            - github.com/corazawaf/coraza-caddy/v2
            - github.com/mholt/caddy-ratelimit
            - github.com/invzhi/caddy-docker-upstreams
            - github.com/hslatman/caddy-crowdsec-bouncer
            - github.com/mholt/caddy-events-exec
            - github.com/caddyserver/forwardproxy
            - github.com/kirsch33/realip
            - github.com/mastercactapus/caddy2-proxyprotocol
            - github.com/sjtug/caddy2-filter
            - github.com/lucaslorentz/caddy-docker-proxy/v2   ← Docker labels 自动反向代理支持
            - github.com/greenpau/caddy-auth-jwt
            - github.com/greenpau/caddy-auth-portal
            - github.com/caddyserver/circuitbreaker
            - github.com/caddyserver/jsonc
            - github.com/caddyserver/json5
            - github.com/WeidiDeng/caddy-prometheus
            - github.com/serfriz/caddy-security-headers
            - github.com/RussellLuo/caddy-ext

            Built with xcaddy v0.4.5 on Go ${{ matrix.go_version }}.

            **Usage notes:**
            - This binary is for ARM64 only (Raspberry Pi 4/5, AWS Graviton, Apple M1/M2 via emulation 等).
            - For Docker usage with caddy-docker-proxy: use CMD ["caddy", "docker-proxy", "--watch"] 或类似。
            - Check SHA256: `sha256sum caddy-arm64` after download.

            Latest caddy-docker-proxy integration uses v2.11.0 (compatible with Caddy 2.11.1).
